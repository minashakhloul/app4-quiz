script(type='text/javascript').
	
	$(start);
	
	function start() {
		var socket = io.connect('http://localhost:3000/poke');
		$("#poke-btn").click(function() {
			socket.emit('poke', { my: 'data' });
		});
	};
	
h1= title

button(id="poke-btn", class="btn", data-action="bea").
	POKE
